import { createContext } from "react";

/**
 * Context object for managing notes throughout the application
 * Created using React's createContext API
 * @type {React.Context}
 */
const noteContext = createContext();

export default noteContext;
